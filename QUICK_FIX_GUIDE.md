# ⚡ БЫСТРОЕ ИСПРАВЛЕНИЕ ПЕРЕД ЮKASSA

## 🎯 Что нужно сделать (5 минут)

### Шаг 1: Автоматическое исправление email адресов

```bash
# Сначала проверим что будет изменено (без реальных изменений)
python scripts/fix_fake_emails.py --dry-run

# Если все ок, применяем изменения
python scripts/fix_fake_emails.py
```

Это заменит все фейковые email (support@lootlink.com, tech@lootlink.com и т.д.) на реальный: **ivanpetrov20066.ip@gmail.com**

### Шаг 2: Удалить тестового пользователя DemoSeller

```bash
python scripts/cleanup_production.py
```

### Шаг 3: Обновить .env файл

Добавьте или обновите в `.env`:

```env
DEFAULT_FROM_EMAIL=ivanpetrov20066.ip@gmail.com
```

### Шаг 4: Проверить изменения

```bash
# Посмотреть что изменилось
git diff

# Проверить сайт
python manage.py runserver
# Откройте: http://localhost:8000/requisites/
```

### Шаг 5: Применить изменения на сервере

```bash
# Если изменения корректны, закоммитьте
git add .
git commit -m "fix: replace fake emails with real contact email"
git push

# На сервере:
cd /var/www/lootlink
git pull
sudo systemctl restart lootlink
```

---

## ✅ Готово!

После выполнения этих шагов ваш сайт полностью готов для отправки в ЮKassa.

### Что было исправлено:

✅ Заменены все фейковые email на реальные  
✅ Удален тестовый пользователь DemoSeller  
✅ Обновлен DEFAULT_FROM_EMAIL  

### Что проверить перед отправкой:

- [ ] Откройте http://91.218.245.178/requisites/
- [ ] Проверьте что все данные корректны:
  - ФИО: Петров Иван Дмитриевич
  - ИНН: 773474557248
  - Email: ivanpetrov20066.ip@gmail.com
  - Телефон: +7 (926) 926-99-88

---

## 📞 Если что-то пошло не так

1. **Откатить изменения:**
   ```bash
   git checkout .
   ```

2. **Восстановить файлы:**
   ```bash
   git reset --hard HEAD
   ```

3. **Связаться:**
   - Email: ivanpetrov20066.ip@gmail.com
   - Телефон: +7 (926) 926-99-88

---

## 📋 Подробный отчет

Полный отчет по проверке сайта: [SITE_VALIDATION_REPORT.md](SITE_VALIDATION_REPORT.md)

