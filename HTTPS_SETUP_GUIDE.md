# 🔐 Руководство по настройке HTTPS для LootLink

**Дата:** 6 ноября 2025  
**Версия:** 1.0

---

## 📋 Содержание

1. [Быстрый старт](#быстрый-старт)
2. [Что будет установлено](#что-будет-установлено)
3. [Самоподписанный сертификат](#самоподписанный-сертификат)
4. [Переход на Let's Encrypt](#переход-на-lets-encrypt)
5. [Устранение неполадок](#устранение-неполадок)

---

## 🚀 Быстрый старт

### Автоматическая установка (рекомендуется)

```powershell
# Запустите из корня проекта
.\setup_https_production.ps1
```

Скрипт автоматически:
- ✅ Создаст SSL сертификат
- ✅ Настроит Nginx для HTTPS
- ✅ Обновит настройки Django
- ✅ Перезапустит сервисы
- ✅ Настроит редирект с HTTP на HTTPS

### Ручная установка

```bash
# 1. Подключитесь к серверу
ssh root@91.218.245.178

# 2. Загрузите и запустите скрипт
cd /opt/lootlink
curl -O https://raw.githubusercontent.com/reazonvan/LootLink---Marketplace/main/scripts/setup_https.sh
chmod +x setup_https.sh
./setup_https.sh

# 3. Обновите Django настройки
./scripts/enable_django_https.sh
```

---

## 📦 Что будет установлено

### SSL Сертификат

- **Тип:** Самоподписанный (Self-Signed)
- **Срок действия:** 365 дней
- **Расположение:**
  - Приватный ключ: `/etc/nginx/ssl/lootlink.key`
  - Сертификат: `/etc/nginx/ssl/lootlink.crt`

### Nginx Configuration

- **HTTP (порт 80):** Автоматический редирект на HTTPS
- **HTTPS (порт 443):** Основной сервер
- **Протоколы:** TLS 1.2, TLS 1.3
- **WebSocket:** Поддержка через `wss://`

### Django Settings

Будут добавлены в `.env`:
```bash
SECURE_SSL_REDIRECT=True
SESSION_COOKIE_SECURE=True
CSRF_COOKIE_SECURE=True
```

### Security Headers

```
Strict-Transport-Security: max-age=31536000
X-Frame-Options: SAMEORIGIN
X-Content-Type-Options: nosniff
X-XSS-Protection: 1; mode=block
Referrer-Policy: strict-origin-when-cross-origin
```

---

## ⚠️ Самоподписанный сертификат

### Что это значит?

Самоподписанный сертификат обеспечивает шифрование данных, но **не верифицирован** доверенным центром сертификации.

### Что увидит пользователь?

При первом посещении браузер покажет предупреждение:

**Chrome:**
```
Ваше подключение не защищено
NET::ERR_CERT_AUTHORITY_INVALID
```

**Firefox:**
```
Предупреждение: потенциальная угроза безопасности
```

### Как обойти предупреждение?

Пользователям нужно:
1. Нажать "Дополнительно" / "Advanced"
2. Выбрать "Перейти на сайт" / "Proceed to site"
3. Сертификат будет сохранен в браузере

### Преимущества

✅ Данные передаются в зашифрованном виде  
✅ Защита от прослушивания трафика  
✅ Устранение предупреждения COOP  
✅ Работает WebSocket через WSS  
✅ Бесплатно и быстро настроить

### Недостатки

❌ Предупреждение браузера при первом посещении  
❌ Не подходит для коммерческого использования  
❌ Не повышает доверие пользователей  
❌ Не работает с некоторыми API

---

## 🌐 Переход на Let's Encrypt

Для получения **доверенного** SSL сертификата:

### Шаг 1: Купите доменное имя

Рекомендуемые регистраторы:
- [Reg.ru](https://www.reg.ru) - от 99₽/год (.ru)
- [NameCheap](https://www.namecheap.com) - от $8/год (.com)
- [Cloudflare](https://www.cloudflare.com) - от $9/год

### Шаг 2: Настройте DNS

Добавьте A-запись:
```
Тип: A
Имя: @
Значение: 91.218.245.178
TTL: 3600
```

Для поддомена (например, `www`):
```
Тип: A
Имя: www
Значение: 91.218.245.178
TTL: 3600
```

### Шаг 3: Дождитесь распространения DNS

Проверьте через:
```bash
nslookup yourdomain.com
# Должен вернуть 91.218.245.178
```

Обычно занимает 5-30 минут.

### Шаг 4: Установите Let's Encrypt

```bash
# Подключитесь к серверу
ssh root@91.218.245.178

# Установите Certbot
sudo apt update
sudo apt install certbot python3-certbot-nginx -y

# Получите сертификат
sudo certbot --nginx -d yourdomain.com -d www.yourdomain.com

# Certbot автоматически настроит Nginx
```

### Шаг 5: Автообновление

Certbot автоматически добавит задачу в cron:
```bash
# Проверить таймер
sudo systemctl status certbot.timer

# Тестовое обновление
sudo certbot renew --dry-run
```

---

## 🔧 Устранение неполадок

### Ошибка: "Connection refused"

**Проблема:** Nginx не запущен

**Решение:**
```bash
sudo systemctl status nginx
sudo systemctl start nginx
sudo systemctl enable nginx
```

### Ошибка: "SSL handshake failed"

**Проблема:** Неправильные права на сертификат

**Решение:**
```bash
sudo chmod 600 /etc/nginx/ssl/lootlink.key
sudo chmod 644 /etc/nginx/ssl/lootlink.crt
sudo systemctl reload nginx
```

### Ошибка: "Mixed content"

**Проблема:** Часть ресурсов загружается через HTTP

**Решение:**
Убедитесь, что в шаблонах используются относительные URL:
```html
<!-- ❌ Плохо -->
<img src="http://91.218.245.178/media/image.jpg">

<!-- ✅ Хорошо -->
<img src="/media/image.jpg">
```

### WebSocket не работает

**Проблема:** Используется `ws://` вместо `wss://`

**Решение:**
```javascript
// ❌ Плохо
const ws = new WebSocket('ws://91.218.245.178/ws/chat/');

// ✅ Хорошо
const protocol = window.location.protocol === 'https:' ? 'wss:' : 'ws:';
const ws = new WebSocket(`${protocol}//${window.location.host}/ws/chat/`);
```

### Проверка HTTPS

```bash
# 1. Проверка сертификата
echo | openssl s_client -connect 91.218.245.178:443 2>&1 | grep "Verify return code"

# 2. Проверка конфигурации Nginx
sudo nginx -t

# 3. Проверка портов
sudo netstat -tlnp | grep nginx

# 4. Логи Nginx
sudo tail -f /var/log/nginx/error.log
```

---

## 📊 Сравнение типов сертификатов

| Характеристика | Самоподписанный | Let's Encrypt | Коммерческий |
|---------------|-----------------|---------------|--------------|
| **Стоимость** | Бесплатно | Бесплатно | $50-500/год |
| **Шифрование** | ✅ Да | ✅ Да | ✅ Да |
| **Доверие браузера** | ❌ Нет | ✅ Да | ✅ Да |
| **Срок действия** | Любой | 90 дней | 1-3 года |
| **Автообновление** | Нет | ✅ Да | Нет |
| **Wildcard** | ✅ Да | ✅ Да | ✅ Да |
| **Требует домен** | ❌ Нет | ✅ Да | ✅ Да |
| **Гарантия** | Нет | Нет | До $1M |

---

## 🔄 Обновление сертификата

### Самоподписанный (через год)

```bash
ssh root@91.218.245.178
cd /opt/lootlink
./scripts/setup_https.sh
```

### Let's Encrypt (автоматически)

Certbot автоматически обновляет сертификаты за 30 дней до истечения.

Проверка:
```bash
sudo certbot renew --dry-run
```

---

## 📞 Поддержка

Если возникли проблемы:

1. **Проверьте логи:**
```bash
# Nginx
sudo tail -100 /var/log/nginx/error.log

# Django
sudo journalctl -u lootlink -n 100
```

2. **Проверьте статус сервисов:**
```bash
sudo systemctl status nginx
sudo systemctl status lootlink
```

3. **Откройте Issue** на GitHub с описанием проблемы

---

## ✅ Checklist после установки

- [ ] HTTPS открывается в браузере
- [ ] HTTP редиректит на HTTPS
- [ ] WebSocket (чат) работает
- [ ] Статические файлы загружаются
- [ ] Media файлы доступны
- [ ] Формы отправляются корректно
- [ ] Админ-панель работает
- [ ] Нет предупреждений COOP в консоли

---

**Автор:** LootLink Team  
**Дата обновления:** 6 ноября 2025  
**Версия:** 1.0

