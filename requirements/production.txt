# ==================================================
# PRODUCTION REQUIREMENTS
# ==================================================

-r base.txt

# Production server
gunicorn>=23.0.0

# AWS S3 for media storage — django-storages + boto3 вынесены в base.txt
# (нужны везде, потому что storages в INSTALLED_APPS безусловно)

# Performance and caching
redis>=7.0.0
django-redis>=6.0.0
hiredis>=3.3.0

# Monitoring and error tracking
sentry-sdk>=2.42.0
flower>=2.0.0

