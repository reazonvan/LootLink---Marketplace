# 🪟 РУКОВОДСТВО ДЛЯ WINDOWS

## 🎯 ДВА СЦЕНАРИЯ

### Сценарий 1: У вас Windows, но сайт на Linux сервере (ВАШ СЛУЧАЙ)

**Что делать:**
Все созданные скрипты нужно запускать **НА СЕРВЕРЕ**, а не локально!

```powershell
# 1. Подключитесь к серверу через SSH
ssh root@91.218.245.178

# 2. На сервере выполните команды (это уже Linux!)
cd /var/www/lootlink
python scripts/auto_fix_all.py --auto
sudo systemctl restart lootlink
```

**Скрипты созданы для:**
- ✅ Linux сервер (где работает ваш сайт)
- ✅ Production база данных
- ✅ PostgreSQL на сервере

---

### Сценарий 2: У вас локальная разработка на Windows

Если вы разрабатываете локально на Windows и хотите протестировать:

#### Вариант A: Batch файл

```cmd
cd C:\Users\inexpert\Desktop\LootLink---Marketplace
scripts\auto_fix_all.bat
```

#### Вариант B: PowerShell

```powershell
cd C:\Users\inexpert\Desktop\LootLink---Marketplace
powershell -ExecutionPolicy Bypass -File scripts\auto_fix_all.ps1
```

#### Вариант C: Напрямую Python

```powershell
cd C:\Users\inexpert\Desktop\LootLink---Marketplace

# Проверка БД
python scripts\check_database.py

# Очистка БД
python scripts\auto_fix_all.py --auto
```

---

## 🔧 ДЛЯ PRODUCTION СЕРВЕРА (РЕКОМЕНДУЕТСЯ)

### Способ 1: SSH из Windows

**Используйте Windows Terminal или PowerShell:**

```powershell
# Подключение к серверу
ssh root@91.218.245.178

# После подключения вы на Linux!
cd /var/www/lootlink

# Запустите Python скрипт
python scripts/auto_fix_all.py --auto

# Или bash скрипт
bash scripts/auto_cleanup.sh

# Перезапуск сервиса
sudo systemctl restart lootlink
```

### Способ 2: SSH клиент (PuTTY)

1. Откройте PuTTY
2. Подключитесь к `91.218.245.178`
3. Логин: `root`
4. Выполните команды выше

### Способ 3: VS Code Remote SSH

1. Установите расширение "Remote - SSH" в VS Code
2. Подключитесь к серверу
3. Откройте терминал в VS Code (на сервере)
4. Выполните команды

---

## 📋 ЧТО ВАМ НУЖНО СДЕЛАТЬ (ПОШАГОВО)

### ШАГ 1: Загрузить изменения на сервер

**Через Git (рекомендуется):**

```powershell
# На Windows (локально)
cd C:\Users\inexpert\Desktop\LootLink---Marketplace

# Добавить все файлы
git add .

# Закоммитить
git commit -m "Add database cleanup scripts"

# Отправить на GitHub
git push origin main
```

### ШАГ 2: Подключиться к серверу

```powershell
# Из PowerShell на Windows
ssh root@91.218.245.178
```

### ШАГ 3: Обновить код на сервере

```bash
# На сервере (Linux)
cd /var/www/lootlink
git pull origin main
```

### ШАГ 4: Запустить очистку

```bash
# На сервере (Linux)
python scripts/auto_fix_all.py --auto
```

### ШАГ 5: Перезапустить сервис

```bash
# На сервере (Linux)
sudo systemctl restart lootlink
```

---

## 🛠️ ЕСЛИ НЕТ SSH КЛИЕНТА

### Установите один из:

**1. Windows Terminal + OpenSSH (встроено в Windows 10/11):**
```powershell
# Проверьте что SSH установлен
ssh -V

# Если нет, установите:
# Настройки → Приложения → Дополнительные компоненты → OpenSSH Client
```

**2. PuTTY (бесплатно):**
- Скачать: https://www.putty.org/
- Установить и запустить
- Ввести IP: 91.218.245.178

**3. VS Code Remote SSH:**
- Установить VS Code
- Установить расширение "Remote - SSH"
- Подключиться к серверу

---

## 📊 СХЕМА РАБОТЫ

```
┌─────────────────┐
│ Windows (ваш ПК)│
│                 │
│ 1. Git commit   │
│ 2. Git push     │
│ 3. SSH connect  │
└────────┬────────┘
         │
         │ Internet
         ▼
┌─────────────────────┐
│ Linux Server        │
│ (91.218.245.178)    │
│                     │
│ 4. git pull         │
│ 5. python scripts/  │
│ 6. systemctl restart│
└─────────────────────┘
```

---

## ✅ БЫСТРАЯ КОМАНДА (ВСЁ СРАЗУ)

### На Windows (локально):

```powershell
# Сохраните как quick_deploy.ps1
git add .
git commit -m "Database cleanup ready"
git push origin main

# Теперь подключитесь к серверу:
ssh root@91.218.245.178
```

### На сервере (после SSH):

```bash
cd /var/www/lootlink && \
git pull origin main && \
python scripts/auto_fix_all.py --auto && \
sudo systemctl restart lootlink && \
echo "✅ ГОТОВО!"
```

---

## 🎯 ИТОГОВАЯ ИНСТРУКЦИЯ ДЛЯ ВАС

Поскольку у вас **Windows локально** и **Linux на сервере**:

### 1. На Windows (сейчас):

```powershell
# Откройте PowerShell в папке проекта
cd C:\Users\inexpert\Desktop\LootLink---Marketplace

# Закоммитьте изменения
git add .
git commit -m "Add cleanup scripts and docs"
git push origin main
```

### 2. Подключитесь к серверу:

```powershell
ssh root@91.218.245.178
```

### 3. На сервере:

```bash
# Обновить код
cd /var/www/lootlink
git pull origin main

# Запустить очистку
python scripts/auto_fix_all.py --auto

# Перезапустить
sudo systemctl restart lootlink

# Проверить
curl http://91.218.245.178/requisites/
```

---

## 🆘 ПРОБЛЕМЫ И РЕШЕНИЯ

### "ssh: command not found"

**Решение:**
```powershell
# Установите OpenSSH Client:
# Пуск → Настройки → Приложения → Дополнительные компоненты
# Найдите "OpenSSH Client" и установите
```

### "Permission denied (publickey)"

**Решение:**
```powershell
# Используйте пароль вместо ключа:
ssh -o PreferredAuthentications=password root@91.218.245.178
```

### "Python not found" на Windows

**Решение:**
```powershell
# Проверьте установку Python:
python --version

# Или используйте:
py --version
py scripts\check_database.py
```

---

## 📄 СОЗДАННЫЕ ФАЙЛЫ

### Для Linux сервера (основные):
- ✅ `scripts/auto_fix_all.py` - Python скрипт (работает везде)
- ✅ `scripts/check_database.py` - проверка БД
- ✅ `scripts/cleanup_database.py` - очистка БД
- ✅ `scripts/auto_cleanup.sh` - bash скрипт
- ✅ `scripts/check_db.sql` - SQL проверка
- ✅ `scripts/cleanup_db.sql` - SQL очистка

### Для Windows (дополнительно):
- ✅ `scripts/auto_fix_all.bat` - Windows batch
- ✅ `scripts/auto_fix_all.ps1` - PowerShell
- ✅ `WINDOWS_GUIDE.md` - это руководство

---

## 🎉 РЕЗЮМЕ

**У вас Windows** → Вам нужно:
1. Закоммитить изменения (Git)
2. Подключиться к серверу (SSH)
3. Запустить скрипты **НА СЕРВЕРЕ**

**Скрипты работают на сервере (Linux), а не локально!**

---

**Контакты:**
- Email: ivanpetrov20066.ip@gmail.com
- Телефон: +7 (926) 926-99-88

