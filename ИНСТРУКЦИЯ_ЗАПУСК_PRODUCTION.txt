═══════════════════════════════════════════════════════════════════════════
  LootLink - Полный Запуск Production Сервера
═══════════════════════════════════════════════════════════════════════════

ЕДИНСТВЕННАЯ ИНСТРУКЦИЯ ДЛЯ ЗАПУСКА И ОБСЛУЖИВАНИЯ PRODUCTION СЕРВЕРА

Дата: 5 ноября 2025
Версия: 1.0.0

═══════════════════════════════════════════════════════════════════════════


┌───────────────────────────────────────────────────────────────────────────┐
│  🚀 БЫСТРЫЙ СТАРТ - ЗАПУСК PRODUCTION                                     │
└───────────────────────────────────────────────────────────────────────────┘

1. Подключитесь к серверу:
   
   ssh root@91.218.245.178


2. Перейдите в каталог проекта:
   
   cd /opt/lootlink


3. Запустите скрипт полного деплоя:
   
   sudo bash deploy_production_full.sh


4. Готово! Скрипт выполнит все 14 шагов автоматически.


═══════════════════════════════════════════════════════════════════════════


┌───────────────────────────────────────────────────────────────────────────┐
│  📋 ЧТО ДЕЛАЕТ СКРИПТ deploy_production_full.sh                           │
└───────────────────────────────────────────────────────────────────────────┘

Скрипт выполняет полный production deployment в 14 шагов:

Шаг 1:  Проверка прав доступа (root)
        → Проверяет, что скрипт запущен с sudo

Шаг 2:  Проверка структуры проекта
        → Проверяет наличие каталогов: /opt/lootlink, venv, .env
        → Проверяет наличие manage.py

Шаг 3:  Проверка системных сервисов
        → PostgreSQL - запускает если не работает
        → Redis - пытается запустить (опционально)
        → Nginx - запускает если не работает

Шаг 4:  Проверка настроек безопасности
        → DEBUG=False (критично!)
        → SECRET_KEY изменен (критично!)
        → ALLOWED_HOSTS настроен

Шаг 5:  Проверка подключения к базе данных
        → Тестирует подключение через Django
        → Проверяет количество таблиц в БД

Шаг 6:  Обновление кода из Git
        → Делает git fetch
        → Создает backup branch перед обновлением
        → Выполняет git reset --hard origin/main

Шаг 7:  Установка/обновление зависимостей Python
        → Обновляет pip
        → Устанавливает пакеты из requirements.txt

Шаг 8:  Применение миграций базы данных
        → АВТОМАТИЧЕСКИЙ БЭКАП БД перед миграциями!
        → Применяет все неприменённые миграции
        → Бэкап сохраняется в /var/backups/lootlink/

Шаг 9:  Сборка статических файлов
        → Очищает старую статику
        → python manage.py collectstatic --noinput --clear
        → Устанавливает правильные права (www-data:www-data)

Шаг 10: Очистка кеша
        → Очищает Django cache
        → Очищает Redis cache (если доступен)

Шаг 11: Перезапуск сервисов
        → systemctl daemon-reload
        → Перезапуск lootlink (Gunicorn/Django)
        → Перезагрузка Nginx
        → Перезапуск Celery (если используется)

Шаг 12: Проверка работоспособности
        → Проверка статусов всех сервисов
        → HTTP тест localhost
        → HTTP тест внешнего IP (91.218.245.178)

Шаг 13: Анализ логов
        → Последние 10 строк lootlink
        → Последние 10 строк Nginx errors

Шаг 14: Генерация финального отчета
        → Создает отчет с результатами
        → Сохраняет в deployment_report_ДАТА_ВРЕМЯ.txt


═══════════════════════════════════════════════════════════════════════════


┌───────────────────────────────────────────────────────────────────────────┐
│  📊 ЛОГИРОВАНИЕ                                                           │
└───────────────────────────────────────────────────────────────────────────┘

Каждый запуск скрипта создает ДЕТАЛЬНЫЕ ЛОГИ:

1. ПОЛНЫЙ ЛОГ DEPLOYMENT
   Путь: /var/log/lootlink/deployment_ГГГГММДД_ЧЧММСС.log
   
   Содержит:
   - Все выполняемые команды
   - Результаты каждого шага
   - Временные метки всех действий
   - Подробный вывод команд
   
   Формат:
   [2025-11-05 14:30:15] [INFO] Система логирования инициализирована
   [2025-11-05 14:30:15] [STEP] Шаг 1/14: Проверка прав доступа
   [2025-11-05 14:30:15] [SUCCESS] Проверка прав root пройдена
   [2025-11-05 14:30:16] [WARNING] Redis не запущен
   [2025-11-05 14:30:17] [ERROR] Не удалось запустить сервис


2. ЛОГ ОШИБОК И ПРЕДУПРЕЖДЕНИЙ
   Путь: /var/log/lootlink/deployment_errors_ГГГГММДД_ЧЧММСС.log
   
   Содержит только:
   - Ошибки (ERROR)
   - Предупреждения (WARNING)


3. ФИНАЛЬНЫЙ ОТЧЕТ
   Путь: /opt/lootlink/deployment_report_ГГГГММДД_ЧЧММСС.txt
   
   Содержит:
   - Дата и время завершения
   - Длительность деплоя
   - Количество шагов/ошибок/предупреждений
   - Статус всех сервисов
   - URL доступа к сайту
   - Пути к логам


4. ССЫЛКА НА ПОСЛЕДНИЙ ОТЧЕТ
   Путь: /opt/lootlink/.last_deployment_report
   
   Команда для просмотра:
   cat $(cat /opt/lootlink/.last_deployment_report)


═══════════════════════════════════════════════════════════════════════════


┌───────────────────────────────────────────────────────────────────────────┐
│  🔍 ПРОСМОТР ЛОГОВ                                                        │
└───────────────────────────────────────────────────────────────────────────┘

ПОСЛЕ ДЕПЛОЯ - Просмотр результатов:
───────────────────────────────────────

# Последний отчет deployment
cat $(cat /opt/lootlink/.last_deployment_report)

# Последний полный лог deployment
ls -t /var/log/lootlink/deployment_*.log | head -1 | xargs cat

# Последние ошибки deployment
ls -t /var/log/lootlink/deployment_errors_*.log | head -1 | xargs cat


В РЕАЛЬНОМ ВРЕМЕНИ - Мониторинг работы:
────────────────────────────────────────

# Логи Django/Gunicorn (в реальном времени)
sudo journalctl -u lootlink -f

# Последние 50 строк логов Django
sudo journalctl -u lootlink -n 50

# Логи Nginx (ошибки)
sudo tail -f /var/log/nginx/lootlink-error.log

# Логи Nginx (доступ)
sudo tail -f /var/log/nginx/lootlink-access.log


ПОИСК ПРОБЛЕМ:
──────────────

# Только ошибки из логов Django
sudo journalctl -u lootlink -p err -n 20

# Все логи за последний час
sudo journalctl -u lootlink --since "1 hour ago"

# Логи за конкретную дату
sudo journalctl -u lootlink --since "2025-11-05 14:00:00"


═══════════════════════════════════════════════════════════════════════════


┌───────────────────────────────────────────────────────────────────────────┐
│  💾 АВТОМАТИЧЕСКИЕ БЭКАПЫ                                                 │
└───────────────────────────────────────────────────────────────────────────┘

Скрипт АВТОМАТИЧЕСКИ создает бэкапы:

1. БЭКАП БАЗЫ ДАННЫХ
   Когда: Перед применением миграций (Шаг 8)
   Путь: /var/backups/lootlink/pre_migration_ГГГГММДД_ЧЧММСС.sql.gz
   
   Восстановление:
   gunzip -c /var/backups/lootlink/pre_migration_XXXXXXXX.sql.gz | \
       sudo -u postgres psql lootlink_db


2. БЭКАП КОДА (GIT)
   Когда: Перед обновлением кода из Git (Шаг 6)
   Что: Создается backup branch
   Имя: backup_ГГГГММДД_ЧЧММСС
   
   Восстановление:
   cd /opt/lootlink
   git checkout backup_XXXXXXXX


РУЧНОЙ БЭКАП:
──────────────

# Бэкап базы данных
sudo -u postgres pg_dump lootlink_db | \
    gzip > /var/backups/lootlink/manual_$(date +%Y%m%d).sql.gz

# Бэкап медиа файлов
tar -czf /var/backups/lootlink/media_$(date +%Y%m%d).tar.gz \
    /opt/lootlink/media/

# Бэкап конфигурации
cp /opt/lootlink/.env /var/backups/lootlink/env_$(date +%Y%m%d).backup


═══════════════════════════════════════════════════════════════════════════


┌───────────────────────────────────────────────────────────────────────────┐
│  ⚙️ УПРАВЛЕНИЕ СЕРВИСАМИ                                                  │
└───────────────────────────────────────────────────────────────────────────┘

СТАТУС СЕРВИСОВ:
────────────────

# Проверка всех сервисов
sudo systemctl status lootlink nginx postgresql redis

# Только lootlink (Django/Gunicorn)
sudo systemctl status lootlink

# Краткий статус (active/inactive)
systemctl is-active lootlink nginx postgresql


ПЕРЕЗАПУСК:
───────────

# Полный перезапуск (Django + Nginx)
sudo systemctl restart lootlink nginx

# Только Django
sudo systemctl restart lootlink

# Только Nginx (без перерыва)
sudo systemctl reload nginx

# PostgreSQL (осторожно!)
sudo systemctl restart postgresql


ЗАПУСК/ОСТАНОВКА:
─────────────────

# Запуск сервиса
sudo systemctl start lootlink

# Остановка сервиса
sudo systemctl stop lootlink

# Автозапуск при загрузке системы
sudo systemctl enable lootlink

# Отключить автозапуск
sudo systemctl disable lootlink


ПРОВЕРКА КОНФИГУРАЦИИ:
──────────────────────

# Проверка конфигурации Nginx
sudo nginx -t

# Проверка Django
cd /opt/lootlink
source venv/bin/activate
python manage.py check

# Проверка БД подключения
python manage.py check --database default


═══════════════════════════════════════════════════════════════════════════


┌───────────────────────────────────────────────────────────────────────────┐
│  🔒 ПРОВЕРКА БЕЗОПАСНОСТИ                                                 │
└───────────────────────────────────────────────────────────────────────────┘

КРИТИЧНЫЕ НАСТРОЙКИ:
────────────────────

1. DEBUG должен быть False
   
   Проверка:
   grep "DEBUG=" /opt/lootlink/.env
   
   Ожидается: DEBUG=False
   
   Исправление:
   cd /opt/lootlink
   nano .env
   # Изменить DEBUG=False
   sudo systemctl restart lootlink


2. SECRET_KEY должен быть уникальным
   
   Проверка:
   grep "SECRET_KEY=" /opt/lootlink/.env
   
   НЕ должно быть: django-insecure-dev-key-change-in-production
   
   Генерация нового ключа:
   cd /opt/lootlink
   source venv/bin/activate
   python -c "from django.core.management.utils import get_random_secret_key; \
       print(get_random_secret_key())"
   
   # Скопировать сгенерированный ключ
   nano .env
   # Вставить: SECRET_KEY=новый_ключ
   sudo systemctl restart lootlink


3. ALLOWED_HOSTS настроен
   
   Проверка:
   grep "ALLOWED_HOSTS=" /opt/lootlink/.env
   
   Должно быть:
   ALLOWED_HOSTS=localhost,127.0.0.1,91.218.245.178,ваш-домен.com


АВТОМАТИЧЕСКАЯ ПРОВЕРКА:
────────────────────────

Скрипт deploy_production_full.sh автоматически проверяет все эти
параметры на Шаге 4 и выдает ошибки/предупреждения.


═══════════════════════════════════════════════════════════════════════════


┌───────────────────────────────────────────────────────────────────────────┐
│  🛠️ ЧАСТЫЕ ОПЕРАЦИИ                                                       │
└───────────────────────────────────────────────────────────────────────────┘

БЫСТРЫЙ ПЕРЕЗАПУСК (без деплоя):
─────────────────────────────────

ssh root@91.218.245.178 "sudo systemctl restart lootlink nginx"


ТОЛЬКО ОБНОВЛЕНИЕ КОДА:
────────────────────────

ssh root@91.218.245.178 << 'EOF'
cd /opt/lootlink
git pull origin main
source venv/bin/activate
python manage.py migrate
python manage.py collectstatic --noinput
sudo systemctl restart lootlink
EOF


ОЧИСТКА КЕША:
─────────────

ssh root@91.218.245.178 << 'EOF'
cd /opt/lootlink
source venv/bin/activate
python scripts/clear_cache.py
EOF


СОЗДАНИЕ СУПЕРПОЛЬЗОВАТЕЛЯ:
────────────────────────────

ssh root@91.218.245.178 << 'EOF'
cd /opt/lootlink
source venv/bin/activate
python manage.py createsuperuser
EOF


ПРИМЕНЕНИЕ МИГРАЦИЙ:
────────────────────

ssh root@91.218.245.178 << 'EOF'
cd /opt/lootlink
source venv/bin/activate
python manage.py migrate
sudo systemctl restart lootlink
EOF


СБОРКА СТАТИКИ:
───────────────

ssh root@91.218.245.178 << 'EOF'
cd /opt/lootlink
source venv/bin/activate
python manage.py collectstatic --noinput --clear
sudo systemctl reload nginx
EOF


═══════════════════════════════════════════════════════════════════════════


┌───────────────────────────────────────────────────────────────────────────┐
│  🐛 РЕШЕНИЕ ПРОБЛЕМ                                                       │
└───────────────────────────────────────────────────────────────────────────┘

ПРОБЛЕМА: Сайт не открывается
──────────────────────────────

1. Проверить статус сервисов:
   sudo systemctl status lootlink nginx

2. Посмотреть логи:
   sudo journalctl -u lootlink -n 50

3. Перезапустить:
   sudo systemctl restart lootlink nginx

4. Проверить порты:
   sudo netstat -tlnp | grep -E ':(80|443|8000)'

5. Проверить firewall:
   sudo ufw status


ПРОБЛЕМА: 500 Internal Server Error
────────────────────────────────────

1. Смотрим логи Django:
   sudo journalctl -u lootlink -n 100

2. Смотрим логи приложения:
   tail -f /opt/lootlink/logs/errors.log

3. Проверяем права:
   sudo chown -R www-data:www-data /opt/lootlink

4. Проверяем .env:
   cat /opt/lootlink/.env | grep -E "(DEBUG|SECRET_KEY|DB_)"

5. Проверяем Django:
   cd /opt/lootlink
   source venv/bin/activate
   python manage.py check


ПРОБЛЕМА: Статические файлы не загружаются
───────────────────────────────────────────

1. Собрать статику:
   cd /opt/lootlink
   source venv/bin/activate
   python manage.py collectstatic --noinput --clear

2. Проверить права:
   sudo chown -R www-data:www-data /opt/lootlink/staticfiles
   sudo chmod -R 755 /opt/lootlink/staticfiles

3. Проверить конфигурацию Nginx:
   sudo nginx -t

4. Перезагрузить Nginx:
   sudo systemctl reload nginx


ПРОБЛЕМА: База данных недоступна
─────────────────────────────────

1. Проверить PostgreSQL:
   sudo systemctl status postgresql

2. Попробовать подключиться:
   sudo -u postgres psql lootlink_db

3. Проверить .env:
   cat /opt/lootlink/.env | grep DB_

4. Проверить через Django:
   cd /opt/lootlink
   source venv/bin/activate
   python manage.py check --database default

5. Перезапустить PostgreSQL:
   sudo systemctl restart postgresql


ПРОБЛЕМА: Сервис lootlink не запускается
─────────────────────────────────────────

1. Смотрим детальные логи:
   sudo journalctl -u lootlink -n 100 --no-pager

2. Проверяем socket файл:
   ls -la /opt/lootlink/lootlink.sock
   sudo rm /opt/lootlink/lootlink.sock  # если нужно удалить

3. Проверяем права:
   sudo chown -R www-data:www-data /opt/lootlink

4. Пробуем запустить вручную:
   cd /opt/lootlink
   source venv/bin/activate
   gunicorn config.wsgi:application --bind localhost:8000

5. Проверяем конфигурацию systemd:
   cat /etc/systemd/system/lootlink.service
   sudo systemctl daemon-reload


ПРОБЛЕМА: Миграции не применяются
──────────────────────────────────

1. Проверить статус миграций:
   cd /opt/lootlink
   source venv/bin/activate
   python manage.py showmigrations

2. Попробовать применить:
   python manage.py migrate --verbosity 2

3. Откатить последнюю миграцию:
   python manage.py migrate app_name migration_name

4. Создать новые миграции:
   python manage.py makemigrations

5. Проверить БД:
   sudo -u postgres psql lootlink_db -c "\dt"


═══════════════════════════════════════════════════════════════════════════


┌───────────────────────────────────────────────────────────────────────────┐
│  📈 МОНИТОРИНГ И ДИАГНОСТИКА                                              │
└───────────────────────────────────────────────────────────────────────────┘

ИСПОЛЬЗОВАНИЕ РЕСУРСОВ:
───────────────────────

# CPU и память (интерактивно)
htop

# CPU и память (одноразово)
top -bn1 | head -20

# Использование диска
df -h

# Размер каталогов проекта
du -sh /opt/lootlink/*

# Размер базы данных
sudo -u postgres psql -c "SELECT pg_size_pretty(pg_database_size('lootlink_db'));"


СТАТИСТИКА БАЗЫ ДАННЫХ:
────────────────────────

# Количество пользователей
sudo -u postgres psql lootlink_db -c \
    "SELECT COUNT(*) FROM accounts_customuser;"

# Количество объявлений
sudo -u postgres psql lootlink_db -c \
    "SELECT COUNT(*) FROM listings_listing;"

# Количество транзакций
sudo -u postgres psql lootlink_db -c \
    "SELECT COUNT(*) FROM transactions_transaction;"

# Топ 10 таблиц по размеру
sudo -u postgres psql lootlink_db -c \
    "SELECT schemaname, tablename, pg_size_pretty(pg_total_relation_size(schemaname||'.'||tablename)) 
     FROM pg_tables WHERE schemaname = 'public' 
     ORDER BY pg_total_relation_size(schemaname||'.'||tablename) DESC LIMIT 10;"


СЕТЕВАЯ ДИАГНОСТИКА:
────────────────────

# Открытые порты
sudo netstat -tlnp

# Только веб-порты
sudo netstat -tlnp | grep -E ':(80|443|8000)'

# Активные соединения
sudo netstat -ant | grep ESTABLISHED | wc -l

# Проверка доступности сайта
curl -I http://91.218.245.178
curl -I http://localhost


ЛОГИ СИСТЕМЫ:
─────────────

# Размер логов
du -sh /var/log/lootlink/
du -sh /var/log/nginx/

# Старые логи (старше 30 дней)
find /var/log/lootlink/ -name "*.log" -mtime +30

# Очистка старых логов deployment
sudo find /var/log/lootlink/ -name "deployment_*.log" -mtime +30 -delete

# Очистка systemd журнала (старше 7 дней)
sudo journalctl --vacuum-time=7d


═══════════════════════════════════════════════════════════════════════════


┌───────────────────────────────────────────────────────────────────────────┐
│  ✅ CHECKLIST ПОСЛЕ ДЕПЛОЯ                                                │
└───────────────────────────────────────────────────────────────────────────┘

После запуска deploy_production_full.sh проверьте:

□ Сайт открывается: http://91.218.245.178
  curl -I http://91.218.245.178

□ Админка работает: http://91.218.245.178/admin/
  curl -I http://91.218.245.178/admin/

□ Вход работает
  Откройте в браузере и протестируйте

□ Регистрация работает
  Откройте в браузере и протестируйте

□ Статические файлы загружаются
  curl -I http://91.218.245.178/static/css/style.css

□ Нет ошибок в логах
  sudo journalctl -u lootlink -n 50 | grep -i error

□ DEBUG=False
  grep "DEBUG=False" /opt/lootlink/.env

□ SECRET_KEY изменен
  grep "SECRET_KEY=" /opt/lootlink/.env

□ Все сервисы запущены
  sudo systemctl status lootlink nginx postgresql

□ Бэкап создан
  ls -lh /var/backups/lootlink/


═══════════════════════════════════════════════════════════════════════════


┌───────────────────────────────────────────────────────────────────────────┐
│  🎯 РЕКОМЕНДАЦИИ                                                          │
└───────────────────────────────────────────────────────────────────────────┘

СРАЗУ ПОСЛЕ ПЕРВОГО ЗАПУСКА:
────────────────────────────

1. Проверить безопасность:
   - DEBUG=False
   - SECRET_KEY уникальный
   - ALLOWED_HOSTS настроен

2. Настроить автоматические бэкапы:
   sudo crontab -e
   # Добавить:
   0 2 * * * /opt/lootlink/scripts/auto_backup.sh >> /var/log/lootlink-backup.log 2>&1

3. Настроить мониторинг uptime:
   - Зарегистрироваться на uptimerobot.com
   - Добавить монитор для http://91.218.245.178


В ТЕЧЕНИЕ НЕДЕЛИ:
──────────────────

1. Настроить HTTPS (если есть домен):
   sudo apt install certbot python3-certbot-nginx
   sudo certbot --nginx -d ваш-домен.ru

2. Настроить отслеживание ошибок:
   - Зарегистрироваться на sentry.io
   - Добавить SENTRY_DSN в .env

3. Протестировать email:
   cd /opt/lootlink
   source venv/bin/activate
   python scripts/test_sms_email.py


В ТЕЧЕНИЕ МЕСЯЦА:
──────────────────

1. Включить Redis для production:
   В .env: USE_REDIS=True

2. Настроить CDN для статики (опционально)

3. Провести нагрузочное тестирование


═══════════════════════════════════════════════════════════════════════════


┌───────────────────────────────────────────────────────────────────────────┐
│  📞 ПОЛЕЗНАЯ ИНФОРМАЦИЯ                                                   │
└───────────────────────────────────────────────────────────────────────────┘

ПУТИ К ФАЙЛАМ:
───────────────

Проект:          /opt/lootlink
Виртуальное окр: /opt/lootlink/venv
Конфигурация:    /opt/lootlink/.env
Статика:         /opt/lootlink/staticfiles
Медиа:           /opt/lootlink/media
Логи проекта:    /opt/lootlink/logs

Systemd сервис:  /etc/systemd/system/lootlink.service
Nginx конфиг:    /etc/nginx/sites-available/lootlink

Логи deployment: /var/log/lootlink/deployment_*.log
Логи ошибок:     /var/log/lootlink/deployment_errors_*.log
Логи Nginx:      /var/log/nginx/lootlink-*.log

Бэкапы:          /var/backups/lootlink/


URL САЙТА:
──────────

Главная:  http://91.218.245.178
Каталог:  http://91.218.245.178/catalog/
Админ:    http://91.218.245.178/admin/


КОМАНДЫ DJANGO:
───────────────

cd /opt/lootlink
source venv/bin/activate

python manage.py migrate              # Миграции
python manage.py collectstatic        # Статика
python manage.py createsuperuser      # Суперпользователь
python manage.py shell                # Django shell
python manage.py check                # Проверка проекта
python manage.py showmigrations       # Статус миграций
python manage.py dbshell              # Shell БД


КОМАНДЫ GIT:
────────────

cd /opt/lootlink

git status                            # Статус
git log --oneline -10                 # Последние коммиты
git fetch origin                      # Загрузить изменения
git reset --hard origin/main          # Обновить до origin/main
git branch                            # Список веток
git checkout branch_name              # Переключить ветку


═══════════════════════════════════════════════════════════════════════════


┌───────────────────────────────────────────────────────────────────────────┐
│  🎉 ИТОГО                                                                 │
└───────────────────────────────────────────────────────────────────────────┘

ДЛЯ ПОЛНОГО ЗАПУСКА/ОБНОВЛЕНИЯ PRODUCTION:

  ssh root@91.218.245.178
  cd /opt/lootlink
  sudo bash deploy_production_full.sh

Скрипт:
  ✓ Автоматически выполнит все 14 шагов
  ✓ Создаст детальные логи
  ✓ Сделает бэкапы перед критическими операциями
  ✓ Проверит безопасность
  ✓ Перезапустит все сервисы
  ✓ Проверит работоспособность
  ✓ Создаст финальный отчет

Просмотр результатов:
  cat $(cat /opt/lootlink/.last_deployment_report)

Мониторинг в реальном времени:
  sudo journalctl -u lootlink -f


Ваш сайт работает: http://91.218.245.178


═══════════════════════════════════════════════════════════════════════════

Конец инструкции
Версия: 1.0.0
Дата: 5 ноября 2025

